# Sound.back 仕様変更ログ

## 2026年6月11-12日

### 1. Reddit風のポスト タイトル機能追加

**概要**
投稿にタイトル（必須、最大80文字）を追加し、Reddit のような「タイトル + 本文」の構造に変更

**対象コンポーネント**
- 投稿作成・編集ページ: タイトル入力フィールド追加
- 投稿カード表示: タイトルを本文上部に大きく表示
- 投稿詳細ページ: タイトルをヘッダーエリアに表示
- 検索機能: キーワード検索でタイトルも対象に

**ファイル変更**
- `src/pages/CreatePostPage.jsx`: タイトル入力欄追加、80文字制限、バリデーション
- `src/pages/CreatePostPage.css`: タイトル入力フォーム スタイル
- `src/components/PostCard.jsx`: タイトル表示追加
- `src/components/PostCard.css`: タイトル スタイル（18px、700 weight）
- `src/pages/PostDetailPage.jsx`: 詳細ページにタイトル表示
- `src/pages/PostDetailPage.css`: 詳細ページ タイトル スタイル（22px、700 weight）
- `src/pages/SearchPage.jsx`: キーワード検索対象に title を追加
- `firestore.rules`: `title` フィールドをスキーマに追加、必須フィールドとしてバリデーション

**Firestore スキーマ**
```
posts/
  title: string (必須, 1-80 文字)
  body: string (既存)
  ...
```

**DB要件**
- 既存の投稿は `title` フィールドがないが、フロント側で問題なく表示可能
- 新規投稿・編集では `title` が必須

**commit**
- `22da628` - feat: add Reddit-style post titles

---

### 2. 特定UID への管理者ダッシュボードアクセス許可

**概要**
`role === 'admin'` に限定していた運営ダッシュボードアクセスを、指定UIDにも許可する仕組みを追加

**UID**
- `GYA5hIMrHpScR4cHScQ3wNw59v93`

**対象機能**
- マイページに「運営」ボタン表示
- `/admin` ページへのアクセス許可
- X投稿原稿管理画面の表示・編集

**ファイル変更**
- `src/utils/adminAccess.js` (新規): `hasAdminAccess()` ユーティリティ関数、管理者UID を集約管理
- `src/App.jsx`: ページビュー追跡ロジックで `hasAdminAccess()` を使用
- `src/pages/MyPage.jsx`: 「運営」ボタン表示判定で `hasAdminAccess()` を使用
- `src/pages/AdminDashboardPage.jsx`: アクセス制御で `hasAdminAccess()` を使用
- `firestore.rules`: `isAdmin()` 関数を拡張し、指定UIDをホワイトリスト追加

**実装パターン**
```javascript
// src/utils/adminAccess.js
function hasAdminAccess(firebaseUser, userData) {
  const uid = firebaseUser?.uid;
  if (!uid) return false;
  if (userData?.role === 'admin') return true;
  return EXTRA_ADMIN_UIDS.has(uid);
}
```

**Firestore ルール**
```
function isAdmin() {
  return (
      isSignedIn()
      && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin'
    )
    || request.auth.uid in ['GYA5hIMrHpScR4cHScQ3wNw59v93'];
}
```

**commit**
- `f2c4da4` - fix: allow privileged UID to access admin dashboard

---

## デプロイ状況

### 本番反映済み (2026-06-12)
- **Firestore Rules**: ✅ デプロイ完了
- **Hosting**: ✅ デプロイ完了
- **URL**: https://sound-fix-ecfcf.web.app
- **Project**: sound-fix-ecfcf

### ローカル未コミット
- `functions/index.js` (変更なし)
- `OPENAI_COST_SUMMARY.md`

---

## 今後の対応

- [ ] 既存投稿の title フィールド移行（既存投稿は空でもOK）
- [ ] ユーザーへの機能リリースノート作成
- [ ] 特定UID 管理者の権限内容をドキュメント化
